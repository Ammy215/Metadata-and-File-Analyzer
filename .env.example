# Copy this file to .env and fill in a real password before running
# docker-compose up. Backend/Celery-specific settings live in
# backend/.env.example instead (copy that to backend/.env separately).

POSTGRES_DB=fileshield
POSTGRES_USER=fileshield
POSTGRES_PASSWORD=CHANGE_ME_generate_with_openssl_rand_base64_18
